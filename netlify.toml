[build]
  publish = "."

# Enable pretty URLs (removes .html from URLs)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  conditions = {Language = ["en"]}

# Custom 404
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
